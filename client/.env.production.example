# Set this in Vercel to the public Render API origin. The client adds /api/v1.
# Do not include a trailing slash or punctuation.
VITE_API_URL=https://your-render-service.onrender.com
# Use the canonical public storefront origin, without a trailing slash.
PUBLIC_APP_URL=https://your-storefront.example.com