export {};
/**
 * Categories currently expose a read-only collection endpoint, so there is no
 * request input to validate. Category-specific validation belongs in this
 * module when category inputs are introduced.
 */ 
